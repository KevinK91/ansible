object ApiListener "api" {
  cert_path = "{{ icinga2_sysconf_dir }}/pki/{{ inventory_hostname }}.crt"
  key_path = "{{ icinga2_sysconf_dir }}/pki/{{ inventory_hostname }}.key"
  ca_path = "{{ icinga2_sysconf_dir }}/pki/ca.crt"
  accept_commands = true
  accept_config = true
}
